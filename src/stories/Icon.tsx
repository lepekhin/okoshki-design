import { DetailedHTMLProps, HTMLAttributes } from 'react';

export const Icon = (props: DetailedHTMLProps<HTMLAttributes<HTMLImageElement>, HTMLImageElement>) => <img width={24} height={24} alt='' loading='lazy' {...props} />;
